export const config = {
  API_BASE_URL: process.env.NEXT_PUBLIC_API_URL || "http://localhost:5000",
  APP_NAME: "Agency Uptime",
  APP_DESCRIPTION:
    "Professional website monitoring and uptime tracking for digital agencies",
};

export default config;
